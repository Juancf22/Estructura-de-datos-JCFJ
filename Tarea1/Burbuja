package tareauno;

public class Producto {
    int codigo;
    String nombre;
    double precio;
    int cantidad;

    public Producto(int codigo, String nombre, double precio, int cantidad) {
        this.codigo = codigo;
        this.nombre = nombre;
        this.precio = precio;
        this.cantidad = cantidad;
    }

    @Override
    public String toString() {
        return "Código: " + codigo + ", Nombre: " + nombre + ", Precio: $" + precio + ", Cantidad: " + cantidad;
    }
}
package tareauno;

import java.util.Scanner;

public class GestionProductos {

    // Método para buscar un producto por código (búsqueda lineal)
    public static Producto buscarPorCodigo(Producto[] productos, int codigoBusqueda) {
        for (Producto producto : productos) {
            if (producto.codigo == codigoBusqueda) {
                return producto;
            }
        }
        return null;
    }

    // Método para ordenar los productos por precio (usando burbuja)
    public static void ordenarPorPrecio(Producto[] productos) {
        int n = productos.length;
        for (int i = 0; i < n - 1; i++) {
            for (int j = 0; j < n - i - 1; j++) {
                if (productos[j].precio > productos[j + 1].precio) {
                    Producto temp = productos[j];
                    productos[j] = productos[j + 1];
                    productos[j + 1] = temp;
                }
            }
        }
    }

    // Método para ordenar los productos por cantidad (usando burbuja)
    public static void ordenarPorCantidad(Producto[] productos) {
        int n = productos.length;
        for (int i = 0; i < n - 1; i++) {
            for (int j = 0; j < n - i - 1; j++) {
                if (productos[j].cantidad > productos[j + 1].cantidad) {
                    Producto temp = productos[j];
                    productos[j] = productos[j + 1];
                    productos[j + 1] = temp;
                }
            }
        }
    }

    public static void main(String[] args) {
        // Crear un array de productos
        Producto[] productos = {
            new Producto(3005, "Café", 3500.00, 10),
            new Producto(1205, "Fresas con crema", 9200.00, 5),
            new Producto(2002, "Waffles", 13500.00, 8),
            new Producto(4012, "Crepe", 18000.00, 12)
        };

        Scanner scanner = new Scanner(System.in);

        // Opción para ordenar los productos
        System.out.println("Seleccione el criterio de ordenamiento:");
        System.out.println("1. Ordenar por Precio");
        System.out.println("2. Ordenar por Cantidad");
        int opcion = scanner.nextInt();

        if (opcion == 1) {
            ordenarPorPrecio(productos);
            System.out.println("\nProductos ordenados por precio:");
        } else if (opcion == 2) {
            ordenarPorCantidad(productos);
            System.out.println("\nProductos ordenados por cantidad:");
        } else {
            System.out.println("Opción no válida. Mostrando productos sin ordenar.");
        }

        // Mostrar productos ordenados
        for (Producto producto : productos) {
            System.out.println(producto);
        }

        // Pedir al usuario que ingrese un código de producto para buscar
        System.out.print("\nIngrese el código del producto que desea buscar: ");
        int codigoBusqueda = scanner.nextInt();

        // Realizar la búsqueda lineal por código
        Producto productoEncontrado = buscarPorCodigo(productos, codigoBusqueda);

        // Mostrar el resultado de la búsqueda
        if (productoEncontrado != null) {
            System.out.println("\nProducto encontrado: " + productoEncontrado);
        } else {
            System.out.println("\nProducto con código " + codigoBusqueda + " no encontrado.");
        }

        scanner.close();
    }
}
